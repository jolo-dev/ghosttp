{
  "name": "ghosttp",
  "type": "module",
  "version": "0.0.6",
  "node": ">= 18",
  "bin": {
    "ghosttp": "index.js"
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.3",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "h3": "^1.11.1",
    "listhen": "^1.7.2"
  },
  "scripts": {
    "test": "vitest",
    "build": "rm -rf dist/ && tsc ./index.ts --module nodenext --outdir dist --moduleResolution nodenext --resolveJsonModule",
    "pkg": "cp README.md dist/README.md && cp -r ./docs/. dist/docs/"
  }
}